package DSA.Arrays;

public class SpecialIndex {

  /**
   * Given an array, arr[] of size N,
   * the task is to find the count of array indices such that removing an element from these
   * indices makes the sum of even-indexed and odd-indexed array elements equal.

   * Input Format
   * First argument contains an array A of integers of size N

   * Output Format
   * Return the count of array indices such that
   * removing an element from these indices makes the sum of even-indexed and odd-indexed array elements equal.
   */

  static void specialIndex(int [] arr){

  }

  public static void main(String[] args) {

  }

}
